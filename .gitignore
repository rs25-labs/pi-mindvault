# secrets / auth
agent/auth.json
agent/*.key
.env
*.pem

# runtime / cache / large state (never commit)
agent/sessions/
agent/models-store.json
agent/*.log
memory/*.db
memory/*.db-wal
memory/*.db-shm
memory/*.sqlite*
cache/
*.log

# OS
.DS_Store

# worktrees (local isolation, never commit)
.worktrees/
worktrees/
