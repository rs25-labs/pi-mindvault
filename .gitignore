# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# logs
*.log

# worktrees (local isolation, never commit)
.worktrees/
worktrees/

# OS
.DS_Store

# local audit handoff (never commit or publish)
AUDIT-SPEC.md
